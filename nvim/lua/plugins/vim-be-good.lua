return {
  "ThePrimeagen/vim-be-good",
  cmd = "VimBeGood",
  config = function()
    -- Optional: Set custom offset for relative game difficulty
    -- vim.g.vim_be_good_delete_me_offset = 35
    
    -- Optional: Enable logging (useful for debugging)
    -- vim.g.vim_be_good_log_file = 1
  end,
}